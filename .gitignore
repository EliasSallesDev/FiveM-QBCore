# FXServer build output at repo root (if present)
build/

# txData: version server.cfg / resources / config, but not generated caches (Yarn paths
# exceed Windows MAX_PATH; cache is reproducible when the server runs).
txData/**/cache/
txData/**/logs/
txData/**/data/
txData/default/data/
txData/admins.json
txData/distopia/server.cfg
txData/distopia/resources/[[]defaultmaps[]]/forests_city/
txData/distopia/resources/[[]defaultmaps[]]/forests_sa_n/
txData/distopia/resources/[[]defaultmaps[]]/forests_sa_s/